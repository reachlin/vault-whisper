services:
  simulator:
    build:
      context: .
      dockerfile: docker/simulator.Dockerfile
    restart: unless-stopped
    ports:
      - "18080:18080"
    volumes:
      - pet_data:/app/data
    env_file:
      - path: .env
        required: false
    environment:
      - PET_MOCK_HARDWARE=true

  brain:
    build:
      context: .
      dockerfile: docker/brain.Dockerfile
    restart: unless-stopped
    depends_on:
      - simulator
    env_file:
      - path: .env
        required: false
    environment:
      - SIMULATOR_URL=http://simulator:18080
      - MC_BRIDGE_URL=http://host.docker.internal:18090

  test:
    build:
      context: .
      dockerfile: docker/test.Dockerfile
    volumes:
      - .:/app
    environment:
      - PET_MOCK_HARDWARE=true

volumes:
  pet_data:
